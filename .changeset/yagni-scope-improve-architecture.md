---
"mattpocock-skills": minor
---

给 **`improve-codebase-architecture`** 技能的 Explore 步骤添加一个 YAGNI 范围过滤器。它不再均匀地扫描整个仓库，而是把范围收拢到变更实际落地的地方：如果你指明一个方向，它就采纳；否则它会读取最近约 20 条提交信息，把探索偏向活跃开发的路径。在无人触碰的代码里发现的深化机会，是一次你永远兑现不了的重构——杠杆只在你持续编辑的地方才有回报——所以报告不再去整理仓库里那些休眠的角落。
