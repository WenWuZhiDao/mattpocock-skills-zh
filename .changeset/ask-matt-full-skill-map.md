---
"mattpocock-skills": minor
---

让 **`ask-matt`** 路由器跟上完整的技能集。它现在映射了此前缺失的五个技能：**`tdd`**（作为 `implement` 驱动的红-绿引擎，编织进主流程）、**`diagnosing-bugs`**（一个新的"Something's broken"入口——此前没有针对 bug 的路径）、**`domain-modeling`** 和 **`codebase-design`**（一个新的"Vocabulary underneath"小节），以及 **`grilling`**（共享的访谈原语）。`prototype` 被充实为一个独立技能，描述也从"user-invoked skills"拓宽为"the skills"。`CLAUDE.md` 新增了一条维护规则，使今后任何技能的添加/重命名/移除或流程改动都会触发一次 `ask-matt` 复查，与既有的文档页面重新同步规则并列。
