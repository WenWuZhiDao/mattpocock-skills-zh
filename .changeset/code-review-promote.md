---
"mattpocock-skills": minor
---

把处于进行中的 **`review`** 技能重命名为 **`code-review`**，并将它从 `in-progress/` 推广到 `engineering/` bucket。它现在随插件一起发布，列入了顶层和 Engineering 的 README（模型调用），并在 `docs/engineering/code-review.md` 有一个面向人的文档页面。`/implement` 技能和文档现在指向 `/code-review`。
