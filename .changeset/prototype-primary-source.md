---
"mattpocock-skills": minor
---

改写 **`prototype`** 技能围绕单个想法处理其制品的方式：**原型是一手证据**。原型不再是在回答完它的问题后就被删除，而是作为可运行的证据被捕获到一个一次性分支（`prototype/<name>`）上、置于 main 之外，并在实现问题上留下一个指向它的上下文指针——这样 main 分支只保留经过验证的决策，而探索过程仍可被找到。答案（结论 + 问题）依然被持久地捕获在一个 issue／ADR／commit 中。
